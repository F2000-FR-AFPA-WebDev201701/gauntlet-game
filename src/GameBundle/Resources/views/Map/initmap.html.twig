
<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>Gauntlet Alpha</title>
        <link rel="stylesheet" href="{{ asset('stylesheet/style.css') }}" type="text/css"/>
    </head>
    <body>
        <div class="plateau">
            {% for tile in map %}
                <!-- Changer les noms des variables coord/ect-->
                <div class="{{ tile.tuile }} tile" style="margin-left:{{ tile.coordx }}px; margin-top:{{ tile.coordy }}px"></div>
            {% endfor %}
        </div>
    </body>

</html>{# empty Twig template #}
