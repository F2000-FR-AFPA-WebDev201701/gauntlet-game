<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>GantletPHP</title>
        <link rel="stylesheet" type="text/css" href="{{ asset('stylesheet/style.css') }}" />
        <link rel="stylesheet" type="text/css" href="{{ asset('bootstrap-3.3.7-dist/css/bootstrap.min.css') }}" />
        <script src="{{ asset('js/jquery-3.1.1.min.js') }}"></script>       
        <script src="{{ asset('bootstrap-3.3.7-dist/js/bootstrap.min.js') }}"></script>
    </head>
    <body>
        <div class="container">
            <header>
                {% include 'GameBundle:Default:_header.html.twig' %}
            </header>

            <div class="well">
                (main)
            {% block content %} {% endblock %}
            </div>
                
            <div class="well">
                {% include 'GameBundle:Default:_infos.html.twig' %}
            </div>
            
            <footer>
                {% include 'GameBundle:Default:_footer.html.twig' %}
            </footer>
            {% block javascripts %} {% endblock %}
        </div>
    </body>
</html>
